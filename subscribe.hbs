<!DOCTYPE html>
<html>
<head>

    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />

    <title>{{@blog.title}} - {{title}}</title>
    <meta name="author" content="{{author}}">
    <meta name="designer" content="André Borud">
    <meta name="HandheldFriendly" content="True" />

    <link rel="shortcut icon" href="{{asset "images/favicon.png"}}" type="image/png">
    <link rel="stylesheet" type="text/css" href="{{asset "css/screen.min.css"}}" />
    <link rel="stylesheet" type="text/css" href="{{asset "fonts/foundation-icons.css"}}" />

	<link rel="stylesheet" href="{{asset "css/darkula.min.css"}}">
	<script src="{{asset "js/highlight.pack.js"}}"></script>
	<script>hljs.initHighlightingOnLoad();</script>

    <!--[if lt IE 9]>
        <script src="//html5shiv.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    {{ghost_head}}

</head>

<body>

    <header class="main-header" id="top">
        {{#if @blog.logo}}<a href="{{@blog.url}}" alt="{{@blog.title}}"><img src="{{@blog.logo}}" /></a>{{/if}}
    </header>

    {{#if success}}
      <header class="subscribe">
        <h1>Subscribed!</h1>
        <p>
          You've successfully subscribed to <strong>{{@blog.title}}</strong> with the email address <strong>{{email}}</strong>.
        </p>
        <p>
            <a href="{{@blog.url}}">Return to main page</a>
        </p>
      </header>
    {{else}}
      <header class="subscribe">
        <h1>Something went wrong! :(</h1>
        <p>
            <a href="{{@blog.url}}">Return to main page</a>
        </p>
      </header>
    {{/if}}

</body>
</html>
